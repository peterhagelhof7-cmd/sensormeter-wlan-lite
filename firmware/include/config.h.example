#pragma once

// ============================================================================
// Kopiere diese Datei nach "config.h" (wird von Git ignoriert, siehe .gitignore).
//
// Laufzeitkonfiguration (WLAN, Passwoerter, Syslog-Server, ...) kommt gemaess
// Lastenheft aus config.xml auf LittleFS (siehe ConfigManager, volle
// Implementierung ab Phase P2) - NICHT aus dieser Datei. Hier stehen nur
// Werte, die zur Kompilierzeit feststehen muessen.
// ============================================================================

#define DEVICE_FIRMWARE_VERSION "1.0.0"

// Projekt-Kennung fuer die OTA-Herkunftspruefung (siehe OtaManager.h/.cpp) -
// eindeutig je Sensormeter-Projekt, damit sich z.B. die .bin von Sensormeter
// Display (oder des kuenftigen NICHT-Lite sensormeter-wlan) nicht versehentlich
// auf diesem Geraet installieren laesst.
#define FIRMWARE_PROJECT_ID "SENSORMETER-WLAN-LITE"
